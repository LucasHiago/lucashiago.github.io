RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteRule ^(.*)$ https://lucashiago.com/$1 [R,L] 

</IfModule>
    <IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteCond %{HTTPS} !on
    RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI}
</IfModule>
